README for `silence-of-salah-engine` plugins

`silence-of-salah-engine` plugin is a native pipeline for running inference and controlling device behaviour accordingly

Two branches for the plugins

- `flutter-plugin-silence-of-salah-engine`: initially made for flutter
- `tauri-plugin-silence-of-salah-engine`: branched from the flutter plugin